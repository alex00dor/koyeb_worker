{
  "name": "deno CORS proxy application",
  "scripts": {
    "dev": "deno run --watch --allow-net --allow-env main.js",
    "build": "deno compile --allow-net --allow-env --output=cors-deno main.js",
    "start": "./cors-deno"
  },
  "dependencies": {
    "deno-bin": "^1.39.0"
  }
}
